{
  "cache_duration": {
    "type": "int",
    "description": "服务器状态查询的缓存时间（秒）。设为0则禁用缓存。",
    "default": 5
  },
  "disk_paths": {
    "type": "list",
    "editor_mode": true,
    "editor_language": "json",
    "description": "要监控的磁盘路径列表。留空则自动检测所有主要分区。",
    "hint": "示例：[\"/\", \"/home\", \"D:\\\\\"]，请使用JSON数组格式。",
    "default": []
  },
  "show_temp": {
    "type": "bool",
    "description": "是否尝试显示CPU温度（仅在部分Linux系统上有效）。",
    "default": true
  }
}

{
  "cache_duration": {
    "type": "int",
    "description": "服务器状态查询的缓存时间（秒）。设为0则禁用缓存。",
    "default": 5,
    "min": 0,
    "max": 3600
  },
  "disk_paths": {
    "type": "list",
    "editor_mode": true,
    "editor_language": "json",
    "description": "要监控的磁盘路径列表。留空则自动检测所有主要分区。支持 Linux 和 Windows 路径，请使用 JSON 数组格式。",
    "hint": "示例：[\"/\", \"/home\", \"D:\\\\\"]",
    "default": []
  },
  "show_temp": {
    "type": "bool",
    "description": "是否尝试显示CPU温度（仅在部分 Linux 系统和硬件上有效，无效时自动隐藏）。",
    "default": true
  }
}
